query GetBlogs {
    blogs(first: 10) {
        nodes {
            id,
            url,
            posts {
                nodes {
                    id,
                    title,
                    content
                }
            }
        }
    }
}